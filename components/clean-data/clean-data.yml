name: Clean Data
description: Fill Nan values and generate dummy variables

outputs:
  - {
    name: Clean Data, type: LocalPath, description: "Path to processed dataset"
  }

implementation:
  container:
    image: stevedev7/black-friday:clean-data-04
    command: [
      python,
      index.py,
      --clean-data,
      {
        outputPath: Clean Data
      }
    ]